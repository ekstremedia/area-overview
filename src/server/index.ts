/**
 * BFF entry point placeholder. Phase 0 only wires up tooling; the real
 * Fastify server, routes and upstream proxying arrive in phase 2.
 */
import { APP_VERSION } from '../shared/version.js';

console.log(`area-overview server placeholder (v${APP_VERSION})`);
