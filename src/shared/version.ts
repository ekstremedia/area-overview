/**
 * Single source of truth for the app version string, shared between the
 * server and the web bundle. Kept trivial in phase 0 to prove the shared
 * module path alias (@shared/*) and the test runner are both wired up.
 */
export const APP_VERSION = '0.1.0';
