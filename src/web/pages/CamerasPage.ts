/** Placeholder for Phase 8's cameras page. See `placeholder.ts`. */
import { createPlaceholderPage } from './placeholder.js';

export const render = createPlaceholderPage('nav.cameras', 'locality.cameras');
