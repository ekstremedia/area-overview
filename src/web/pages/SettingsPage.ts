/**
 * Placeholder for Phase 9's settings page. Phase 9 also redesigns the
 * masthead for this route (artboards 07/08: title becomes "Innstillinger",
 * a different sub-nav, no locality caption) -- Phase 5 deliberately keeps
 * the shared masthead for every route, including this one, and defers
 * that redesign.
 */
import { createPlaceholderPage } from './placeholder.js';

export const render = createPlaceholderPage('nav.settings', 'locality.settings');
